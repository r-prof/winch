@header@
PKG_CPPFLAGS = -Ilocal/include
PKG_CFLAGS = @cflags@
PKG_LIBS = @libs@ local/lib/libbacktrace.a

all: $(SHLIB)

$(SHLIB): local/lib/libbacktrace.a

local/lib/libbacktrace.a:
	make -C build/libbacktrace install
